#include "main.h"

/**
 * _isdigit - checks if input is in range 0 to 0
 * @c: input to be checked
 * Return: 1 True 0 False
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
		return (1);
	else
		return (0);
}
