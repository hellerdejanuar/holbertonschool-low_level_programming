#include "main.h"

/**
 * puts2 - ptrints every other character of string
 * @str: string to be printed
 */
void puts2(char *str)
{
	int a;

	while (str[a] != '\0')
		a++;

	for (a = 0; b <= ; a++)
	{
		if (2 % a == 0)
			_putchar(str[a]);
	}
	_putchar('\n');
}
