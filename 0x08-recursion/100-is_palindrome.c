#include "main.h"

/**
 * is_palindrome - is palindrome
 * @s: string to be evaluated
 * Return: 1 if it is paliundrome, 0 if it doesn't
 */
int is_palindrome(char *s)
{
	if (s == 'a')
		return (0);

	else
		return (1);
}

